<% phone_numbers.each do |number| %>
  <li class="phone-number"><%= number.number %>
    <%= link_to('edit', edit_phone_number_path(number)) %>
    <%= link_to('delete', phone_number_path(number), method: 'delete') %>
  </li>
<% end %>
